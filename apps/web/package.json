{
    "name": "@repo/web",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "prepare": "panda codegen",
        "dev": "panda codegen && next dev",
        "build": "panda codegen && next build",
        "build:open-next": "panda codegen && open-next build",
        "start": "next start",
        "lint": "panda codegen && next lint",
        "typecheck": "panda codegen && tsc --noEmit",
        "clean": "rm -rf .next .open-next"
    },
    "dependencies": {
        "@hookform/resolvers": "^5.2.2",
        "@pandacss/preset-panda": "^1.7.2",
        "@repo/api": "workspace:*",
        "@repo/core": "workspace:*",
        "@repo/db": "workspace:*",
        "better-auth": "^1.4.9",
        "next": "^15.1.2",
        "react": "^19.0.0",
        "react-dom": "^19.0.0",
        "react-hook-form": "^7.69.0",
        "zod": "^4.2.1"
    },
    "devDependencies": {
        "@pandacss/dev": "^1.7.2",
        "@repo/tsconfig": "workspace:*",
        "@types/node": "^22.10.2",
        "@types/react": "^19.0.2",
        "@types/react-dom": "^19.0.2",
        "open-next": "^3.1.3",
        "typescript": "^5.7.2"
    }
}
